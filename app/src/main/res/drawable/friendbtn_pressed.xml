<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State default HARUS PAKAI CORNERS -->
    <solid android:color="@color/compact04" />
    <!-- Radius konsisten dengan unpressed state -->
    <corners android:radius="10dp"/>
    <!-- Border tetap muncul saat ditekan -->
    <stroke
        android:width="2dp"
        android:color="?attr/colorPrimary" />
</shape>